package myfirstobject.device;
// turnOnDevice(Device device) и turnOffDevice(Device device)
public class Human {
    public void turnOnDevice(Device device) {
        device.turnOn();
    }

    public void turnOffDevice(Device device) {
        device.turnOff();
    }
}
